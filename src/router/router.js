import React from 'react';
import Bundle from './bundle.js';

const routers = [
  // {
  //   path: '/home',
  //   component: (props) => (<Bundle load={() => import('../views/home/home.jsx')}>{(List) => <List {...props}/>}</Bundle>)
  // },
  // {
  //   path: '/login',
  //   component: (props) => (<Bundle load={() => import('../views/login/login.jsx')}>{(List) => <List {...props}/>}</Bundle>)
  // },
];

export default routers;
